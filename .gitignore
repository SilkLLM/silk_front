.env
node_modules
dist
build
*.log
*.env

# Regenerated on every `npm run build` by scripts/generate-sitemap.mjs - its
# lastmod date changes on every build, so committing it just churns diffs.
public/sitemap.xml